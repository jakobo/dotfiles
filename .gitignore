# Lots of junk goes in here.
/caches/
/backups/

# Top secret stuff!
/link/.ssh/id_*
/link/.ssh/*.pem
/link/.ssh/known_hosts*

# gnupg - save config, ignore everything else
/link/.gnupg/*
!/link/.gnupg/gpg.conf
!/link/.gnupg/gpg-agent.conf

# Don't commit Vim plugins, vim-plug will install them.
/link/.vim/plugged

# Don't commit rbenv temp files.
/link/.rbenv/shims/
/link/.rbenv/versions/
/link/.rbenv/version

# Don't commit tmux plugins, tpm will install them.
/link/.tmux/plugins

# Don't commit tmux resurrect state.
/link/.tmux/resurrect

# Ignore any file with LOCAL in the name (good for per-machine vars/functions)
*LOCAL*

# Oh my posh backups
link/.config/oh-my-posh/themes/*.bak

# thefuck caches
link/.config/thefuck/__pycache__